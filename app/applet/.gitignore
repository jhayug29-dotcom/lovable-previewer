# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local
.env
.env.*
!.env.example

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vercel
.vercel

# Local agent/editor session state — machine-specific, not part of the site.
.claude/

# Local verification helpers. These read the live Supabase/Cashfree credentials
# out of the environment, so they stay out of the repo to keep key handling in
# one place (.env, which is already ignored above).
scripts/
